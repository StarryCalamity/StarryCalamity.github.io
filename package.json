{
  "name": "blog",
  "version": "1.0.0",
  "description": "個人開発手帖",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && gh-pages -d dist"
  },
  "dependencies": {
    "astro": "^7.0.3"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
